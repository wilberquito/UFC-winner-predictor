version: "3"
services:

  jupyter:
    image: jupyterlab:latest
    ports:
      - 8888:8888
    depends_on:
      - postgres_db
      - mongo_db

  postgres_db:
    image: postgreslab:latest
    restart: always
    ports:
      - 5432:5432
    hostname: postgresdns
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

  mongo_db:
    image: mongo:latest
    restart: always
    ports:
      - 27017:27017
    hostname: mongodns
    environment:
        MONGO_INITDB_ROOT_USERNAME: mongo
        MONGO_INITDB_ROOT_PASSWORD: mongo

